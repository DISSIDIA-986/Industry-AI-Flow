# Python 3.13 + PaddleOCR专用依赖文件
# 专门为建筑图纸OCR识别设计
# 生成时间: 2025-11-08

# PaddleOCR核心依赖 - Python 3.13 + Nightly build
paddlepaddle>=3.0.0b0
paddleocr>=3.0.0b0

# 图像处理 - 建筑图纸识别必需
pillow==10.0.1
opencv-python==4.8.0.76

# 数值计算和数据处理
numpy==1.24.3
pandas==1.5.3
matplotlib==3.7.2

# NLP和文本处理（用于英文建筑文档分词）
nltk==3.8.1
rank-bm25>=0.2.2

# 异步数据库驱动（P0修复：支持Prompt API连接池）
asyncpg==0.29.0
# 用于FastAPI异步数据库访问，替代psycopg2
# 修复jieba对英文分词bug，替换为NLTK word_tokenize + PorterStemmer

# RAG评估框架（P0优先级：Week 1首要任务）
ragas==0.1.8
datasets>=2.14.0
# 用于测量faithfulness, context_precision, context_recall, answer_relevancy, MRR

# 网络和配置
requests==2.31.0
pyyaml==6.0.1
python-dotenv==1.0.0

# 工具库
tqdm==4.66.1
click==8.1.7

# 开发和测试工具
pytest==7.4.0
pytest-asyncio==0.21.0
black==23.7.0
flake8==6.0.0

# 可选的图像增强依赖
scikit-image==0.21.0
imageio==2.31.5

# Jupyter支持 (可选)
jupyter==1.0.0
notebook==6.5.4
