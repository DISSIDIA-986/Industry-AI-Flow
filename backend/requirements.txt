# ============================================================================
# CRITICAL: Python Version Requirement
# ============================================================================
# This project MUST use Python 3.13.x ONLY
#
# Reason: PaddlePaddle on macOS requires the Developer Nightly Build version,
# which only supports Python 3.9/3.10/3.11/3.12/3.13 (max 3.13).
#
# DO NOT upgrade to Python 3.14 or higher - it will break PaddlePaddle!
# ============================================================================

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database Drivers
psycopg[binary]>=3.1.0
pgvector==0.2.5

# AI/ML Models
sentence-transformers==2.2.2

# Document Processing
PyMuPDF>=1.24.0

# Utilities
requests==2.31.0
pydantic==1.10.13
psutil==5.9.6
python-multipart==0.0.6

# ============================================================================
# PaddlePaddle & PaddleOCR
# ============================================================================
# CRITICAL: Use ONLY the nightly build for macOS compatibility
# Installation command:
#   python -m pip install --pre paddlepaddle -i https://www.paddlepaddle.org.cn/packages/nightly/cpu/
#
# DO NOT change these version specifiers!
# The nightly build is required for macOS ARM64 support.
# ============================================================================
# paddlepaddle==3.3.0.dev (installed via nightly build, not in requirements)
paddleocr>=3.3.0
