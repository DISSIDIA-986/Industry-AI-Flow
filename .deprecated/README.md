# .deprecated

This directory stores soft-deleted artifacts moved from active project paths.

Rules:
- No hard deletion for archived project artifacts.
- Keep paths and move logs traceable via `cleanup_manifest.log`.
- Prefer date-based subfolders for auditability.

Current archived set:
- `.deprecated/reports/2026-02-12/`
